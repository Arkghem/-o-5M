见上方完整计划。概要：离屏两 pass 渲染链路（render → postprocess → 回读校验），分 4 个 Phase：(0) 基础修缮——ResourceDesc 公开化、memory type 查询、修 MeshResource UB；(1) rendergraph 扩展——buffer 资源 + buffer barrier、barrier 布局按用法推导、fence 帧同步；(2) GPU 管线最小封装——RenderPass/Framebuffer 动态创建工具、GraphicsPipeline、shaderc 运行时 GLSL 编译；(3) rhi_verify 搭链路——sceneColor(RGBA16F) + finalColor(RGBA8)，fullscreen triangle render pass、tone map postprocess pass、copyImageToBuffer 回读 + CPU 像素校验退出码；(4) CMake 登记 + 留 transient 池/swapchain/depth prepass 扩展点。教学式：每步概念讲解 + 骨架代码，用户自行补全实现。