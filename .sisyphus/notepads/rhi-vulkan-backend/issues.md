
## 2026-08-06 — rhi_verify VK: task diagnosis was WRONG; 3 real blockers found
- Task claimed: D24_UNORM_S8_UINT unsupported on Apple Silicon → "PSO create failed". **False.** Tested D24 + lazy-create fix → PSO created fine. The real cause of "PSO create failed" was `pso->create()` no-arg STUB always returning false on VK (VKPipeline.h:70 `bool create() override { return false; }`). The test called the interface's no-arg create() unconditionally.
- Blocker 2 (fixed in rhi_verify.cpp): one pipeline reused across TWO incompatible render passes — off-screen FBO (RGBA8_UNORM + depth) vs swapchain (B8G8R8A8_SRGB, NO depth). Vulkan pipelines are baked to one render pass. Fix: second depthless `psoSwap` for the swapchain pass. Validation confirmed: vkCmdDraw render-pass-incompatibility warnings disappeared.
- Blocker 3 (STILL OPEN, backend, out of task scope): swapchain images created WITHOUT `VK_IMAGE_USAGE_TRANSFER_SRC_BIT` (VulkanDevice.cpp:432 — only COLOR_ATTACHMENT|TRANSFER_DST). The VK readback does vkCmdCopyImageToBuffer from the swapchain image → invalid per VUID-vkCmdCopyImageToBuffer-srcImage-00186 → staging gets garbage (observed pixel R=0 G=0 B=124, NOT the sRGB clear 89/89/124). Fix would be in VulkanDevice backend (add TRANSFER_SRC_BIT) — task explicitly forbade backend changes.
- Side observations: present returns VK_ERROR_OUT_OF_DATE_KHR every frame (C2 log-only, pre-existing); GL build crashes with SIGSEGV exit 139 BOTH before and after my changes (verified via git stash → pre-existing, not my regression). The 3-line comment-block in the VK depth branch documents the MoltenVK rationale (kept per task instructions) though the actual cause differs.
